[build]
  base = "frontend"
  publish = "dist"
  command = "npm run build"

[build.environment]
  VITE_API_BASE_URL = "https://supply-chain-risk-analysis-and.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
